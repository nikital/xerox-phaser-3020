# Xerox Phaser 3020 Driver RPMs

This spec packages Xerox Phaser 3020 drivers from the official website into RPMs
for use in immutable distros (like Silverblue, Kinoite, etc).

Checkout the repo and run:
```bash
make
```

Result is written into `out/` directory.
